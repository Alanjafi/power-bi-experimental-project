import "core-js/stable";
import "./../style/visual.less";
import powerbi from "powerbi-visuals-api";
import VisualConstructorOptions = powerbi.extensibility.visual.VisualConstructorOptions;
import VisualUpdateOptions = powerbi.extensibility.visual.VisualUpdateOptions;
import IVisual = powerbi.extensibility.visual.IVisual;
export declare class Visual implements IVisual {
    private visualSettings;
    private formattingSettingsService;
    private svg;
    private iconSvg;
    private logger;
    private container;
    private circle;
    private rect;
    private path;
    private path2;
    private path3;
    private textValue;
    private textLabel;
    constructor(options: VisualConstructorOptions);
    update(options: VisualUpdateOptions): void;
    generateIconSVG(itemType: string, w: number, h: number): void;
    private initiateIconSvg;
    private initiateDataSvg;
    generateDataSvg(name: string, x: number, y: number, val: number): void;
    private clearAttr;
    private createProductIcon;
    private createStateIcon;
    private createSalesIcon;
    private createDatesIcon;
    private createRegionIcon;
    getFormattingModel(): powerbi.visuals.FormattingModel;
}
